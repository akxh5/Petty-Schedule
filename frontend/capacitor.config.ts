import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.akshansh.scheduler',
  appName: 'Faculty Duty Scheduler',
  webDir: 'out'
};

export default config;
